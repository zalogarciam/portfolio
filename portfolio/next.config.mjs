/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: false,
  transpilePackages: ["google-map-react", "typewriter-effect", "react-awesome-slider"],
};

export default nextConfig;
